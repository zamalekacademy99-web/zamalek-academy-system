{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "npm install && npm install --save-dev typescript @types/node && npx prisma generate && npx tsc --project tsconfig.json"
    },
    "deploy": {
        "startCommand": "node dist/server.js",
        "healthcheckPath": "/api/v1/health",
        "restartPolicyType": "ON_FAILURE"
    }
}